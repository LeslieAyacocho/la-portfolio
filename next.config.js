/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/la-portfolio',
    assetPrefix: '/la-portfolio/',
    output: 'export',
    reactStrictMode: true,
}
module.exports = nextConfig
